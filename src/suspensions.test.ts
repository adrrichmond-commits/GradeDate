import { describe, expect, test } from "bun:test";
import { canTransitionAppeal, durationEnds, isSuspensionReason, isSuspensionDuration, APPEAL_TEXT_MAX, APPEAL_TEXT_ADMIN_MAX, truncateAppealText } from "./suspensions";
describe("suspension policy", () => { test("allowlists", () => { expect(isSuspensionReason("underage")).toBe(true); expect(isSuspensionReason("ban")).toBe(false); expect(isSuspensionDuration("30d")).toBe(true); }); test("durations", () => { expect(durationEnds("indefinite")).toBeNull(); expect(durationEnds("24h")).toBeTruthy(); }); test("appeals only pending transition", () => { expect(canTransitionAppeal("pending", "granted")).toBe(true); expect(canTransitionAppeal("granted", "denied")).toBe(false); expect(APPEAL_TEXT_MAX).toBe(2000); }); test("admin queue truncates appeal text to ~600 chars", () => { expect(APPEAL_TEXT_ADMIN_MAX).toBe(600); const short = "I never sent that message."; expect(truncateAppealText(short)).toBe(short); const long = "x".repeat(2000); const out = truncateAppealText(long)!; expect(out.length).toBeLessThanOrEqual(601); expect(out.endsWith("…")).toBe(true); expect(out.slice(0, 600)).toBe("x".repeat(600)); }); test("truncateAppealText handles empty and non-string input", () => { expect(truncateAppealText(null)).toBeNull(); expect(truncateAppealText(undefined)).toBeNull(); expect(truncateAppealText("   ")).toBeNull(); expect(truncateAppealText(42)).toBeNull(); expect(truncateAppealText("  trimmed  ")).toBe("trimmed"); }); });
